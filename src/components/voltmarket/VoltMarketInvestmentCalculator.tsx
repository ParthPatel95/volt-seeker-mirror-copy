import React from 'react';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

const VoltMarketInvestmentCalculator = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Investment Calculator</CardTitle>
        <CardDescription>Calculate investment returns and metrics</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Investment calculator placeholder</p>
      </CardContent>
    </Card>
  );
};

export default VoltMarketInvestmentCalculator;