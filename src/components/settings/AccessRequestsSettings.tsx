import React from 'react';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

const AccessRequestsSettings = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Access Request Settings</CardTitle>
        <CardDescription>Manage access requests and permissions</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Access request settings component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default AccessRequestsSettings;