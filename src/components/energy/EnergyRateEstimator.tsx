import React from 'react';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

const EnergyRateEstimator = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Energy Rate Estimator</CardTitle>
        <CardDescription>Calculate energy rates for your location</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Energy rate estimator component placeholder</p>
      </CardContent>
    </Card>
  );
};

export default EnergyRateEstimator;